services:
    postgres:
        image: postgres:16-alpine
        container_name: pulsestream-postgres
        env_file: .env
        environment:
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
        ports:
            - "5432:5432"
        volumes:
            - postgres_data:/var/lib/postgresql/data
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
            interval: 10s
            timeout: 5s
            retries: 5
        networks:
            - pulsestream

    redis:
        image: redis:7-alpine
        container_name: pulsestream-redis
        command: redis-server --appendonly yes
        ports:
            - "6379:6379"
        volumes:
            - redis_data:/data
        healthcheck:
            test: ["CMD", "redis-cli", "ping"]
            interval: 10s
            timeout: 5s
            retries: 5
        networks:
            - pulsestream

    api:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: pulsestream-api
        command: ["api"]
        env_file: .env
        environment:
            POSTGRES_HOST: postgres
            POSTGRES_PORT: 5432
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
            REDIS_HOST: redis
            REDIS_PORT: 6379
            ENVIRONMENT: ${ENVIRONMENT}
        ports:
            - "8000:8000"
        depends_on:
            postgres:
                condition: service_healthy
            redis:
                condition: service_healthy
        networks:
            - pulsestream

    worker-base:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: pulsestream-worker-base
        command: ["worker", "--base"]
        env_file: .env
        environment:
            POSTGRES_HOST: postgres
            POSTGRES_PORT: 5432
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
            REDIS_HOST: redis
            REDIS_PORT: 6379
            ENVIRONMENT: ${ENVIRONMENT}
        depends_on:
            postgres:
                condition: service_healthy
            redis:
                condition: service_healthy
        networks:
            - pulsestream

    worker-aggregator:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: pulsestream-worker-aggregator
        command: ["worker", "--aggregator"]
        env_file: .env
        environment:
            POSTGRES_HOST: postgres
            POSTGRES_PORT: 5432
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
            REDIS_HOST: redis
            REDIS_PORT: 6379
            ENVIRONMENT: ${ENVIRONMENT}
        depends_on:
            postgres:
                condition: service_healthy
            redis:
                condition: service_healthy
        networks:
            - pulsestream

    # # Optional: Redis Commander for Redis GUI
    # redis-commander:
    #     image: rediscommander/redis-commander:latest
    #     container_name: pulsestream-redis-commander
    #     environment:
    #         - REDIS_HOSTS=local:redis:6379
    #     ports:
    #         - "8081:8081"
    #     depends_on:
    #         - redis
    #     networks:
    #         - pulsestream
    #     profiles:
    #         - tools

    # # Optional: pgAdmin for PostgreSQL GUI
    # pgadmin:
    #     image: dpage/pgadmin4:latest
    #     container_name: pulsestream-pgadmin
    #     environment:
    #         PGADMIN_DEFAULT_EMAIL: admin@pulsestream.local
    #         PGADMIN_DEFAULT_PASSWORD: admin
    #         PGADMIN_CONFIG_SERVER_MODE: "False"
    #     ports:
    #         - "5050:80"
    #     depends_on:
    #         - postgres
    #     networks:
    #         - pulsestream
    #     profiles:
    #         - tools

volumes:
    postgres_data:
    redis_data:

networks:
    pulsestream:
        driver: bridge
